<%= render partial: 'info', locals: { hospital: @hospital } %>
<%= render partial: 'statistics', locals: { hospital_beds: @hospital.hospital_beds } %>
<%= render partial: 'patients', locals: { patients: @patients } %>

<div class="row mt-5">
  <div class="col">
    <%= link_to "ENVIAR RELATÓRIO", "#", class: 'btn btn-lg btn-primary float-right' %>
  </div>
</div>
